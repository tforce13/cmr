import { SelectUserPipe } from './select-user.pipe';

describe('SelectuserPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectUserPipe();
    expect(pipe).toBeTruthy();
  });
});
