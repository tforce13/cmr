import { SelectProfilePipe } from './select-profile.pipe';

describe('SelectProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
