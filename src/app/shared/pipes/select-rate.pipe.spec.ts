import { SelectRatePipe } from './select-rate.pipe';

describe('SelectRatePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectRatePipe();
    expect(pipe).toBeTruthy();
  });
});
